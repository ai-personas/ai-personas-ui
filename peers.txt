# PersonaOS discovery peer list — one node URL per line.
#
# At boot the portal fetches this file, then for every URL below it bootstraps
# that node's .well-known/personaos-discovery.json, runs the provider lookup,
# resolves each record and Ed25519-verifies it in-browser. Blank lines and
# lines starting with '#' are ignored. Trust is the signature, not the host.
#
# NOTE: Cloudflare quick-tunnel URLs ROTATE whenever the node restarts. If the
# one below is dead, add a live node with ?peer=<url> or the "+ PEER" button.
#
# Live PersonaOS node (kernel:dea359c0e1cc67d5) on the current design-alignment
# build (operator bearer-token authority, signed bridging, live cognitive modes,
# sandbox auto-install, deliverable↔pathway coherence, design-derived UI):
https://vanilla-late-insulation-bikini.trycloudflare.com
#
# A visitor's OWN local node, if they run one (harmless when absent).
http://localhost:8765
http://localhost:8908
